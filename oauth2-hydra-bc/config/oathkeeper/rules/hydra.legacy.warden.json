[
  {
    "id": "${OATHKEEPER_RESOURCE_PREFIX}rules:hydra:warden:allowed",
    "matchesMethods": [
      "POST"
    ],
    "matchesUrl": "${OATHKEEPER_PROXY_URL}/warden/allowed",
    "mode": "keto_warden_oauth2",
    "upstream": {
      "url": "${KETO_URL}/warden/subjects/authorize",
      "stripPath": "/warden/allowed"
    },
    "requiredResource": "${HYDRA_RESOURCE_PREFIX}warden:allowed",
    "requiredAction": "${HYDRA_ACTION_PREFIX}decide",
    "requiredScopes": [
      "hydra.warden"
    ]
  },
  {
    "id": "${OATHKEEPER_RESOURCE_PREFIX}rules:hydra:warden:allowed",
    "matchesMethods": [
      "POST"
    ],
    "matchesUrl": "${OATHKEEPER_PROXY_URL}/warden/allowed",
    "mode": "keto_warden_oauth2",
    "upstream": {
      "url": "${KETO_URL}/warden/oauth2/authorize",
      "stripPath": "/warden/allowed"
    },
    "requiredResource": "${HYDRA_RESOURCE_PREFIX}warden:token:allowed",
    "requiredAction": "${HYDRA_ACTION_PREFIX}decide",
    "requiredScopes": [
      "hydra.warden"
    ]
  }
]
