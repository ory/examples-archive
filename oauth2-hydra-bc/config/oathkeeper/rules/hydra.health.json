[
  {
    "id": "${OATHKEEPER_RESOURCE_PREFIX}rules:hydra:health:status",
    "matchesMethods": [
      "GET"
    ],
    "matchesUrl": "${OATHKEEPER_PROXY_URL}//health/status",
    "mode": "pass_through",
    "upstream": {
      "url": "${HYDRA_URL}/"
    }
  }
]
