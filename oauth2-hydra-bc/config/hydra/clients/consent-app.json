{
  "id": "${HYDRA_SUBJECT_PREFIX}${CONSENT_APP_HYDRA_CLIENT_ID}",
  "description": "This client is used by the consent app to interact with ORY Hydra's consent API.",
  "client_secret": "${CONSENT_APP_HYDRA_CLIENT_SECRET}",
  "scope": "hydra.consent",
  "grant_types": [
    "client_credentials"
  ],
  "response_types": [
    "token"
  ]
}
